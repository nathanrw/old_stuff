import math

def Mul2DVector(a,b):
	return [a[0]*b[0],a[1]*b[1]]

while 1:
	try:
		a = input("Input angle in degrees: ")*(math.pi/180)
		b = math.cos(a)
		print b
		print math.acos(b)*(180/math.pi)
		print ""
	except:
		print "Failed."
		print ""